import styled from "styled-components";

const ContainerWrapper = styled.div.attrs({
  className: "container",
})`
  max-width: 960px;
  margin-right: auto;
  margin-left: auto;
  padding-right: 15px;
  padding-left: 15px;
`;

export { ContainerWrapper };
